/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "zeowquchdsbljoujlhmr.supabase.co" },
    ],
  },
};

export default nextConfig;
